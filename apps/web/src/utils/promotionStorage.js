import { createPromotionLocalState, webPromotionStorage } from '@nail-couture/shared/utils/promotionLocalState.js';

export const webPromotionLocalState = createPromotionLocalState(webPromotionStorage);
